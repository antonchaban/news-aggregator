/*
Package web provides HTTPs handlers and routing for managing articles and sources
in a news aggregator application. The package uses the Gin web framework to define
routes and handle HTTPs requests.
*/
package web
